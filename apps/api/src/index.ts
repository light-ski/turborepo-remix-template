import { App, CodedError } from "@slack/bolt";

import { slackAppOptions } from "./slack-setup";

require("dotenv").config();

// const slackApp = new App(slackAppOptions());

(async () => {
  // await slackApp.start();
  // console.log(`⚡️ Bolt app has started!`);
})();
